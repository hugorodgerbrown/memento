[project]
name = "memento-distiller"
version = "0.1.0"
description = "Memento's distiller: a scheduled MCP client that structures the inbox with one chosen model (ADR 0013, 0019)."
requires-python = ">=3.14"
dependencies = [
    "anthropic[mcp]>=1.0",
    "mcp>=2.2,<3",
]

[tool.uv]
package = false
